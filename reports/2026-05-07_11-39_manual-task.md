# AI Provider Selection Report

## Task Summary

Add a Settings option to choose OpenAI or DeepSeek as the text AI provider, with provider-specific API key labeling, base URL defaults, and model defaults. Keep correction and summary API calls unimplemented for now.

## Commit Hash

5103aa2

## Files Changed

- `src/shared/types.ts`
- `src/shared/settings.ts`
- `src/shared/readiness.ts`
- `src/main/store.ts`
- `src/renderer/components/SettingsScreen.tsx`
- `src/renderer/components/Dashboard.tsx`

## Verification Commands Run

- `npm test`
- `npm run typecheck`
- `npm run build`

## Automated Test Result

- `npm test`: passed, 3 tests passed.
- `npm run typecheck`: passed for renderer, main, and preload TypeScript projects.
- `npm run build`: passed for main, preload, and renderer bundles.

## UI Areas Affected

- Settings AI provider section
- Settings validation messages
- First-launch setup readiness
- Dashboard setup copy and correction action copy

## Manual UI Check

1. Run `npm run dev`.
2. Open the Electron window.
3. Go to Settings.
4. Confirm the AI provider section shows OpenAI and DeepSeek choices.
5. Select OpenAI and confirm the base URL is `https://api.openai.com/v1` when the current base URL was still the previous default.
6. Select DeepSeek and confirm the base URL changes to `https://api.deepseek.com` when the current base URL was still the previous provider default.
7. Confirm the API key label changes between OpenAI API key and DeepSeek API key.
8. Confirm correction and summary model fields remain editable after changing provider.
9. Save with empty API key and confirm validation reports AI provider API key as required.
10. Save with a non-HTTPS base URL and confirm validation reports that the base URL must be HTTPS.
11. Save valid placeholder settings and confirm the settings persist after switching away from Settings and back.

## Not Verified

- I could not verify the Electron UI manually in this environment during this run.
- I did not call OpenAI or DeepSeek APIs because this task only adds provider selection settings.
- I did not verify real correction or summary generation because those flows are still not implemented.

## Failures Found And Fixed

- No command failures were found.
- During review, the Settings field wrapper was changed from `label` to `div` so the provider buttons are not nested inside an interactive label wrapper.

## Remaining Risks

- Existing stored settings are normalized from the old `openaiApiKey` key into `aiApiKey`, but a manual app launch should verify migration against a real user settings file.
- DeepSeek model names remain editable. The default uses `deepseek-chat`, but the API implementation step should confirm the final production model names before making network calls.

## Recommended Follow-Up Prompt

Implement AI correction provider adapters for OpenAI and DeepSeek.

Context:
- observed behavior: Settings can now choose OpenAI or DeepSeek and stores provider-neutral API key, base URL, and model names
- expected behavior: the Correct transcript action should call the selected provider through an OpenAI-compatible chat completions adapter in the Electron main process
- affected files: `src/shared/types.ts`, `src/shared/settings.ts`, `src/main`, `src/preload/preload.ts`, `src/renderer/components/Dashboard.tsx`
- verification required: `npm test`, `npm run typecheck`, `npm run build`, and manual UI checks for both providers using fake and real credentials where available

Follow `PROMPT_EXECUTION_RULES.md` and `UI_VERIFICATION.md`.

## Recommended Larger-Model Prompt

Use a larger reasoning model to review and implement the AI provider adapter boundary.

Focus on:
- keeping API keys in the Electron main process
- avoiding raw audio transfer to any AI provider
- OpenAI-compatible request/response parsing
- provider-specific base URL and model handling
- user-visible error messages for failed correction calls

Inputs to read first:
- `PROJECT.md`
- `ARCHITECTURE.md`
- `PROMPT_EXECUTION_RULES.md`
- `OPENAI_AND_PROMPTS.md`
- this report file

Expected output:
- implemented correction adapter
- tests passing
- updated report
- git commit
